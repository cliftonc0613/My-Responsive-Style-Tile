# Required
# --------

# Live Server
scheme: sftp
host: styletile.ctwdsdemos.com
username: styletile
password: Tigers~13!
# Optional
# --------

# Remote path
path: public_html/ProPod

# Remote file name in which the current revision is stored
revision_file: .revision

# These files (from Git) will not be uploaded during a deploy
exclude:
    - .gitignore
    - dandelion_staging.yml
    - dandelion.yml
    - package.json
    - LICENSE
    - node_modules
    - task
    - .sass-cache
    - README.md
